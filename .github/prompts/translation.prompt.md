ensure all translation keys in the provided pages and its nested components are exists in frontend\messages\ar.json and frontend\messages\en.json. If any key is missing, add it in both files and if you find any hardcoded text in the pages that should be translated, create a new key for it in both files.